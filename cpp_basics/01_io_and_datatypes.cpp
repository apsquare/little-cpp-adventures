/*
 * C++ BASICS - input, output and datatypes
 *
 * Topic : cin / cout, reading a full line with getline, and the sizes of the
 *         built-in types (char, int, double).
 * Note  : Practice snippets, kept commented out for reference.
 */

#include <iostream>
#include<string>

using namespace std ;

int main (){
  // int a , b;
  // std::cout << "Please enter number a" << std::endl;
  // std::cin >> a;
  // std::cout << "Please enter number b" << std::endl;
  // std::cin >> b;

  // cout << "The Sum of the numbers is " << a + b << endl;
  // cout << "The Difference of the numbers is " << a - b << endl;
  // cout << "The product of the numbers is " << a * b << endl;
  // cout << "The quotient of the numbers is " << a / b << endl;
  

  // Get the full name and print it
  // string name ; 
  // std::cout << "Enter your name please . " <<std::endl;
  // getline(cin,name);
  // cout << name << endl;

  // char - 1 byte
  // 4 bytes
  // 8 bytes

  // 2
  // 2.5


}
